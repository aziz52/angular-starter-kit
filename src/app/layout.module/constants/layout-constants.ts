import { BaseConstants } from '@app/core.module';

export class LayoutConstants extends BaseConstants {

    public static routerPaths = {
        userAccount: 'user',
        noMatch: '**'
    };

    public static apiEndPoints = {

    };

}
